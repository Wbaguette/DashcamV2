# TODO


Properly inject warning manager
Better error handling on JSON decoder, lets try to NOT use exceptions/throwables

Recordings download client:
    1. Fix SSL cert 
    2. map API responses:
        500 (recording files corrupted)
        404 (no timespans found)
        400 (malformed request)
        200 (timespans found)

USB camera -> can we get a smaller cord? that'd probably help it
CSI camera -> can we get a smaller CSI cord?


Calculate reasonable retention rate of videos
